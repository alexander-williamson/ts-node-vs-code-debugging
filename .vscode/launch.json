{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program (app)",
      "runtimeArgs": ["-r", "ts-node/register"],
      "cwd": "${workspaceFolder}/app",
      "args": ["${file}"]
    }
  ]
}
